# 스크립트 변수 제어

스크립트를 사용하여 변수를 프로그래밍적으로 제어하는 방법을 알아봅니다.

## 개요

스크립트 변수 제어를 사용하면 복잡한 로직을 구현하고, 변수 값을 조건에 따라 자동으로 변경할 수 있습니다.

## 기본 스크립트 문법

### 변수 읽기

```javascript
const 현재값 = getVariable("변수명");
```

### 변수 쓰기

```javascript
setVariable("변수명", 새값);
```

### 변수 삭제

```javascript
deleteVariable("변수명");
```

## 실용 예시

### HP 관리 스크립트

```javascript
// HP 감소 함수
function 데미지받기(피해량) {
  const 현재HP = getVariable("HP");
  const 새HP = Math.max(0, 현재HP - 피해량);
  setVariable("HP", 새HP);
  
  if (새HP === 0) {
    setVariable("상태", "쓰러짐");
  }
}
```

### 조건부 변수 업데이트

```javascript
// 시간에 따른 상태 변경
function 시간업데이트() {
  const 시간 = getVariable("시간");
  
  if (시간 >= 6 && 시간 < 18) {
    setVariable("배경", "낮");
  } else {
    setVariable("배경", "밤");
  }
}
```

## 이벤트 리스너

변수 변경 시 자동으로 실행되는 스크립트를 설정할 수 있습니다:

```javascript
onVariableChange("HP", (oldValue, newValue) => {
  if (newValue < oldValue) {
    console.log("데미지를 받았습니다!");
  }
});
```

!!! info "디버깅"
    `console.log()`를 사용하여 스크립트 실행 과정을 확인할 수 있습니다.

## 다음 단계

상태바를 통해 변수 상태를 시각적으로 확인하려면 [상태바](상태바.md)를 확인하세요.

# 상태바

변수 상태를 실시간으로 확인할 수 있는 상태바 기능을 알아봅니다.

## 상태바란?

상태바는 주요 변수의 현재 값을 화면에 항상 표시해주는 UI 요소입니다. 게임의 HP 바나 경험치 바처럼 중요한 정보를 시각적으로 확인할 수 있습니다.

## 상태바 활성화

설정에서 상태바를 활성화합니다:

```
설정 > 표시 > 상태바 활성화
```

## 상태바 설정

### 표시할 변수 추가

```javascript
{
  "statusBar": [
    {
      "variable": "HP",
      "label": "체력",
      "type": "bar",
      "color": "#ff4444"
    },
    {
      "variable": "골드",
      "label": "💰",
      "type": "text"
    }
  ]
}
```

### 표시 타입

| 타입 | 설명 | 용도 |
|------|------|------|
| `bar` | 프로그레스 바 | HP, MP, 경험치 등 |
| `text` | 텍스트 표시 | 골드, 이름 등 |
| `icon` | 아이콘 표시 | 상태 이상, 버프 등 |
| `badge` | 뱃지 표시 | 알림, 카운터 등 |

## 커스터마이징

### 색상 설정

```javascript
{
  "variable": "HP",
  "color": "#ff4444",
  "backgroundColor": "#330000",
  "warningThreshold": 30  // 30% 이하일 때 깜빡임
}
```

### 위치 설정

```javascript
{
  "position": "top",  // top, bottom, left, right
  "align": "center"   // start, center, end
}
```

!!! tip "추천 설정"
    자주 확인해야 하는 변수만 상태바에 추가하세요. 너무 많은 정보는 오히려 혼란을 줄 수 있습니다.

## 다음 단계

변수 구조를 체계적으로 작성하려면 [변수 구조 작성](07-변수-구조-작성.md)을 확인하세요.
